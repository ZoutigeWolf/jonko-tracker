function highlightSelectedSidebarButton() {
    let selectedSidebarButton = document.getElementById("current-sidebar-button");

    if (selectedSidebarButton !== null) {
        selectedSidebarButton.style.backgroundColor = "#499b4a";
    }
};